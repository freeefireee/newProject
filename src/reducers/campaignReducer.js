const initialState = {
    campaigns: [],
  };
  
  const campaignReducer = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  
  export default campaignReducer;
  